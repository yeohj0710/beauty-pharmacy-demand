# 제품 수요 조사 에이전트 지침

## 지금 할 일

이 저장소에서 "데이터 조사" "수집" "갱신" 류의 요청을 받았다면, 더 물어보지
말고 아래를 그대로 실행한다. 무엇을 얼마나 수집할지는 `brief`가 현재 데이터를
보고 알려주므로 따로 물어볼 필요가 없다.

```bash
git pull github main
npm install
npm run brief          # 조사 규칙 + 오늘의 작업 목록이 한 번에 출력된다
```

`npm run brief` 출력이 이번 작업의 지시서다. 거기 적힌 우선순위(P0→P1→P2→P3)
순서로 수집하고, 끝나면 같은 출력에 적힌 완료 파이프라인을 돌린 뒤 커밋·푸시한다.
배포(`npx vercel --prod`)는 명시적으로 요청받았을 때만 한다.

상세 절차는 [docs/recollection-runbook.md](docs/recollection-runbook.md),
플랫폼별 기준은 [docs/collection-protocol.md](docs/collection-protocol.md)에 있다.

## 목적

등록 제품의 온라인 관심 신호를 5개 플랫폼에서 같은 재현 절차로 조사한다. 내부 매출·판매량·순이익·이익률은 읽거나 저장하거나 공개하지 않는다. 제품명과 공개 웹 정보만 사용한다.

## 실행 전

1. `docs/collection-protocol.md`를 끝까지 읽는다.
2. `app/product-catalog.json`에서는 제품명만 사용한다.
3. `app/demand-entities.json`의 통합 SKU, 대표 검색어, 제외어를 우선 사용한다.
4. 로그인 또는 보안 확인이 필요하면 멈추고 사용자에게 해당 브라우저에서 로그인을 요청한다. 비밀번호·쿠키·세션 저장소를 읽지 않는다.

## 제품별 조사 순서

1. 대표 검색어 2~4개를 확정한다: 소비자 통칭, 브랜드+제품군, 한글/영문 변형.
2. 각 검색어로 플랫폼 검색을 실행한다.
3. 검색 결과 앞 20개를 검토하고 콘텐츠 ID 또는 정규화 URL로 중복을 제거한다.
4. 관련성 기준을 통과한 자연 검색 결과를 최대 10개 채택한다.
5. 원시 지표, 검색어, 순위, 게시일, 원본 URL, 수집 시각을 그대로 기록한다.
6. 광고·협찬·브랜드 공식 콘텐츠는 삭제하지 말고 별도 플래그로 구분한다.
7. 결과가 10개보다 적으면 실제 채택 수를 남긴다. 빈 값을 0으로 만들지 않는다.

## 관련성 판정

다음 중 하나를 만족해야 포함한다.

- 제목·본문·해시태그·자막·영상 화면에서 대상 제품을 명확히 식별할 수 있다.
- 브랜드명과 제품군이 함께 나타나 대상 제품임을 합리적으로 확인할 수 있다.
- 제품의 공식 별칭 또는 등록된 대표 검색어가 콘텐츠에서 동일한 의미로 사용된다.

다음은 제외한다.

- 성분이나 효능만 같고 대상 제품을 특정할 수 없는 콘텐츠
- 동명이인·다른 제품·오탈자 검색 결과
- 단순 판매 페이지, 품절 안내, 가격 비교만 있는 결과
- 동일 콘텐츠의 재업로드·퍼가기·URL 매개변수만 다른 중복
- 검색 결과에 노출되지 않았는데 임의로 추가한 콘텐츠

관련성이 애매하면 포함하지 말고 `relevance: uncertain`으로 후보 목록에 남긴다.

## 데이터 원칙

- 합계만 쓰지 않는다. `표본 수`, `합계`, `중앙값`, `최댓값`, `게시 후 일평균`을 함께 계산한다.
- 한 콘텐츠가 전체 조회수의 70% 이상이면 `outlierDominated: true`로 표시한다.
- 플랫폼 간 원시 숫자를 직접 더하지 않는다. 플랫폼별 점수를 따로 계산한 뒤 비교한다.
- 접근 불가·비공개·숨김 값은 `null`이다. 추정값이나 임의 숫자를 넣지 않는다.
- 모든 수치는 원본 URL과 수집 시각으로 감사 가능해야 한다.
- 자동 수집 결과는 사람이 관련성 표본 3개 이상을 확인하기 전까지 `verified`로 표시하지 않는다.

## 완료 조건

- 채택 콘텐츠마다 URL과 관련성 근거가 있다.
- 중복 제거가 완료됐다.
- 광고/협찬/공식 계정 여부가 분리됐다.
- 플랫폼별 필수 지표와 누락 사유가 기록됐다.
- 조사 JSON이 `docs/collection-protocol.md`의 레코드 형식을 따른다.

## 수집 후 필수 파이프라인

`app/signals.json`을 갱신했다면 아래 세 명령을 순서대로 실행하고 전부
성공한 상태에서만 커밋한다.

```bash
npm run validate:signals   # 데이터 계약 검증 — ERROR 0건 필수
npm run audit:signals      # 통계 보정 파일(signal-quality.json) 재생성
npm run test:unit          # 회귀 테스트
```

- `app/signal-quality.json`은 생성 파일이다. 직접 편집하지 않는다.
- 재수집 작업의 범위·우선순위·절대 규칙은
  `docs/recollection-runbook.md`를 따른다.
